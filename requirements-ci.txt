# CI installs a CPU-only torch from the PyTorch index first, then this file.
# Deriving from requirements.txt instead of restating it means a dependency
# added there cannot pass CI while breaking every developer. The torch pin in
# requirements.txt is already satisfied by the pre-installed CPU build, so pip
# leaves it alone.
-r requirements.txt
